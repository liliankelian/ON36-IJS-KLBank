import { AccountType } from "../../../../commom/types/account.type";

export class Account {
   
        ID: number; 
        balance:number; 
        clientID: number;
        type: AccountType
   
}